<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 只有兩個權限。刻意唔要 MEDIA_CONTENT_CONTROL（signature-level，普通 app 攞唔到）、
         RECEIVE_BOOT_COMPLETED（系統會自動拉返 NotificationListenerService）、RECORD_AUDIO。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:theme="@style/Theme.AutoLyrics"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false"
        android:supportsRtl="true">

        <!-- android:resource（唔係 android:value）—— value 會當佢係字串
             "@xml/automotive_app_desc" 傳過去，唔會 resolve 成個 XML resource。 -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Android Auto 入口。descriptor 係 <uses name="media"/>，所以呢度要
             MediaBrowserService；唔可以同時再聲明 CarAppService + template，
             兩邊矛盾嘅話個 app 唔會喺 launcher 出現。 -->
        <service
            android:name=".auto.LyricsBrowserService"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- 舊嘅 template 路線，留住做參考但唔再聲明畀 host。
             POI + template 試過：icon 加齊、manifest 一致，個 app 一樣唔會喺
             Customize launcher 出現 —— 「歌詞顯示」唔屬於任何 template category。
             enabled=false 令佢唔會同上面個 MediaBrowserService 撞。 -->
        <service
            android:name=".car.LyricsCarAppService"
            android:enabled="false"
            android:exported="false" />

        <service
            android:name=".media.NotificationMediaWatcher"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".phone.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
