<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 只有兩個權限。刻意唔要 MEDIA_CONTENT_CONTROL（signature-level，普通 app 攞唔到）、
         RECEIVE_BOOT_COMPLETED（系統會自動拉返 NotificationListenerService）、RECORD_AUDIO。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false"
        android:supportsRtl="true">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:value="@xml/automotive_app_desc" />

        <service
            android:name=".car.LyricsCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <!-- POI + res/xml/automotive_app_desc.xml 嘅 <uses name="template"/>
                     必須夾。試過改做 MEDIA 但冇同步改 descriptor，兩邊矛盾，
                     Android Auto 索性唔列出個 app。
                     亦都唔行 MEDIA 呢條路：MEDIA app 要實作 MediaBrowserService
                     （曲目瀏覽 + 播放控制），但呢個 app 唔播歌，佢淨係讀其他
                     播放器嘅狀態嚟顯示歌詞，冇 library 可以 browse。 -->
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>

        <service
            android:name=".media.NotificationMediaWatcher"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".phone.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
